export type NetworkInfo = {
  headquarters: string;
  homepage: string;
  id: number;
  logo_path: string;
  name: string;
  origin_country: string; // You might want to replace this with the actual data type if known
};